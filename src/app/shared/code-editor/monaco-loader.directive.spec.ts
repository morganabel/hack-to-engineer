import { MonacoLoaderDirective } from './monaco-loader.directive';

describe('MonacoLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new MonacoLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
